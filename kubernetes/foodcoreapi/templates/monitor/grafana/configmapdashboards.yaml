apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-config-dashs
  labels:
    app: {{ include "grafana.name" . }}
  namespace: {{ .Values.namespace.monitor.name }}
data:
  jvm_micrometer_dash.json: |-
{{ .Files.Get "assets/grafana/dashboards/jvm_micrometer_dash.json" | indent 4 }}