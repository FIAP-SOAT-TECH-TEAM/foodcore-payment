kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "fluentd.fullname" . }}-clusterrolebinding
roleRef:
  kind: ClusterRole
  name: {{ include "fluentd.fullname" . }}-clusterrole
  apiGroup: rbac.authorization.k8s.io
subjects:
- kind: ServiceAccount
  name: {{ include "fluentd.fullname" . }}-serviceaccount
  namespace: {{ .Values.namespace.efk.name }}