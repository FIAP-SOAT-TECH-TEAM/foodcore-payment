apiVersion: v1
kind: Service
metadata:
  name: {{ include "kibana.fullname" . }}-service
  labels:
    app: {{ include "kibana.name" . }}
  namespace: {{ .Values.namespace.efk.name }}
spec:
  selector:
    app: {{ include "kibana.name" . }}
  ports:
    - port: {{ .Values.kibana.ports.port }}
      targetPort: {{ .Values.kibana.ports.targetPort }}
      name: {{ .Values.kibana.ports.name }}