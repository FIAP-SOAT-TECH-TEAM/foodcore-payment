{{- if .Values.ingressInt.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ingressInt.fullname" . }}
  labels:
    app: {{ include "ingressInt.name" . }}
  {{- with .Values.ingressInt.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingressInt.className }}
  ingressClassName: {{ .Values.ingressInt.className }}
  {{- end }}
  {{- if .Values.ingressInt.tls }}
  tls:
    {{- range .Values.ingressInt.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressInt.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- $services := (include "ingressInt.services" $ | fromYamlArray) }}
          {{- range $svc := $services }}
          - path: {{ $svc.path }}
            pathType: {{ $svc.pathType }}
            backend:
              service:
                name: {{ $svc.name }}
                port:
                  number: {{ $svc.port }}
          {{- end }}
    {{- end }}
{{- end }}